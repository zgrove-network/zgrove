{
  "name": "@zgrove/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "zgrove-worker": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@zgrove/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
