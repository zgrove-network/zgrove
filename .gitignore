node_modules/
dist/
*.tsbuildinfo

# SQLite lives outside version control; it is the accounting source of truth.
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Local assistant instructions, not project documentation. The project's own
# rules live in docs/OVERVIEW.md.
CLAUDE.md

# Wallet keys and pool credentials never enter the repo.
.env
.env.*
!.env.example
secrets/

.DS_Store

# Rust build output
crates/**/target/
